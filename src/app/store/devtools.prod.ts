import { EnvironmentProviders } from '@angular/core';

/** The production stand-in for `devtools.ts`: a shipped build has no Redux DevTools. */
export const storeDevtools: EnvironmentProviders[] = [];
