User-agent: *
Allow: /

# Signed-in zones. All three are guarded, so a crawler only ever gets an empty
# shell from them — keep them out of the crawl budget entirely.
Disallow: /auth/
Disallow: /admin/
Disallow: /user/

Sitemap: https://beehivemind.tech/sitemap.xml
